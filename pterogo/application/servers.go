package application

// Servers represents the servers in the Pterodactyl panel
type Servers struct {
}

// ServersApplication is a client for the Servers API
type ServersApplication struct {
	client *Application
}
